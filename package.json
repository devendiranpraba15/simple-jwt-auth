{
  "name": "simple-jwt-auth",
  "version": "1.0.0",
  "description": "Base Project",
  "main": "index.js",
  "scripts": {
    "install-all": "pnpm --workspace-root --recursive install",
    "dev-run": "pnpm --parallel --filter \"backend\" --filter \"frontend\" dev",
    "build-all": "pnpm --parallel --filter \"./**\" build",
    "stop": "kill-port --port 3000,5000",
    "lint": "eslint .",
    "lint:fix": "eslint --fix",
    "format": "prettier --write './**/*.{js,jsx,ts,tsx,css,md,json}' --config ./.prettierrc"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@babel/preset-env": "^7.23.2",
    "@babel/preset-react": "^7.23.3",
    "regenerator-runtime": "^0.14.0",
    "kill-port": "^2.0.1",
    "cross-env": "^7.0.3"
  },
  "dependencies": {
    "eslint": "^7.32.0",
    "eslint-config-prettier": "^9.1.0",
    "eslint-plugin-prettier": "^5.1.3",
    "eslint-plugin-react": "^7.34.0",
    "prettier": "^3.2.5"
  }
}
